{
  "name": "boq-agents-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "frontend": "vite",
    "backend": "py -3 -m uvicorn backend.main:app --host 127.0.0.1 --port 8000",
    "backend:watch": "py -3 -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.19"
  }
}
